export interface FormInputs {
  name: string;
  email: string;
  phoneNumber: string;
  message: string;
}

export interface SendEmail {
  name: string;
  email: string;
  phoneNumber: string;
  message: string;
  token: string;
}
